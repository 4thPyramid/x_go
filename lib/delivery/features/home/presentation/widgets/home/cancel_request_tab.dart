import 'package:flutter/material.dart';

class CancelOrderRequestTab extends StatelessWidget {
  const CancelOrderRequestTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Add more widgets here as needed
        Text(
          'Cancel Order Request Tab',
          style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
        ),
        // You can add more widgets to display cancel order requests
      ],
    );
  }
}
